#!/bin/bash

set -e
cd "$(dirname "$0")"/..

[ $# = 1 ] || {
  echo "Usage: $0 <commit message>"
  exit 1
}

commit_message="$1"

git add -A :/
git commit -m "$commit_message"
git push
sleep 3
script/status